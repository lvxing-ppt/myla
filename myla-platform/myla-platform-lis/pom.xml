<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.myla</groupId>
        <artifactId>myla-platform</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <artifactId>myla-platform-lis</artifactId>
    <name>MYLA Platform LIS (LIS 对接)</name>
    <description>LIS/HIS 系统对接 — LisInboundService(二期入站医嘱接口) + OutboundMessageConsumer(MQ消费/重试3次/DLQ) + LisGatewayService(出站消息管理) + lis_config 字段映射配置表</description>

    <dependencies>
        <dependency>
            <groupId>com.myla</groupId>
            <artifactId>myla-platform-sample</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.myla</groupId>
            <artifactId>myla-platform-result</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-amqp</artifactId>
        </dependency>
        <!-- HAPI HL7 v2.x parser -->
        <dependency>
            <groupId>ca.uhn.hapi</groupId>
            <artifactId>hapi-base</artifactId>
            <version>2.3</version>
        </dependency>
        <dependency>
            <groupId>ca.uhn.hapi</groupId>
            <artifactId>hapi-structures-v25</artifactId>
            <version>2.3</version>
        </dependency>
    </dependencies>
</project>
