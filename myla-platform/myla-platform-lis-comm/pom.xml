<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.myla</groupId>
        <artifactId>myla-platform</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <artifactId>myla-platform-lis-comm</artifactId>
    <name>MYLA Platform LIS Communication (通讯层)</name>
    <description>统一通讯层独立应用 — LIS 入站/出站 + 仪器网关。与业务层通过 RabbitMQ 解耦，零业务依赖。</description>

    <dependencies>
        <!-- 仪器网关 SPI + 通道 + 分桢器 + 解析器 -->
        <dependency>
            <groupId>com.myla</groupId>
            <artifactId>myla-platform-gateway</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 共享 DTO (UnifiedResult / LabEvent / 枚举) -->
        <dependency>
            <groupId>com.myla</groupId>
            <artifactId>myla-common</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- RabbitMQ -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-amqp</artifactId>
        </dependency>
        <!-- JSON -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <!-- Netty (网关 NettyTcpChannel 需要) -->
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-all</artifactId>
            <version>4.1.107.Final</version>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.myla.lis.comm.LisCommApplication</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
